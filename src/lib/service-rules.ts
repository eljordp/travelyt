export const AIRLINE_BAG_CUTOFF_MINUTES = 40;

export const AIRLINE_CUTOFF_COPY =
  "Pickup timing is confirmed from airport distance, traffic, and airline baggage acceptance rules.";

export const AIRLINE_CUTOFF_DETAIL =
  `Travelyt targets airline bag acceptance before the airline cutoff. Default cutoff is ${AIRLINE_BAG_CUTOFF_MINUTES} minutes before departure unless an airline or airport approves a Travelyt-specific exception.`;
